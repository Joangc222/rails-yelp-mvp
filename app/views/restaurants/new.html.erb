<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.select :category, collection: Restaurant::CATEGORIES %><br />
  <%= f.submit "Submit restaurant", class: "btn btn-primary" %>
<% end %>
